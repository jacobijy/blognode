import * as Actions from '../actions/editorActions';
const editor = (
    state = {
    }, action) => {
        switch (action.type) {
            case Actions.Editor_New_Article_Request:
                
                break;
        
            default:
                break;
        }
    }